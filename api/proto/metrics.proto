syntax = "proto3";

package api.proto;

option go_package = "github.com/a-palonska/metrics-server/gen/proto";

message Metric {
  string id = 1;
  string m_type = 2;
  optional int64 delta = 3;
  optional double value = 4;
}

message CheckConnectionRequest {
}

message CheckConnectionResponse {
    int32 status = 1;
}

message UpdateMetricRequest {
    Metric metric = 1;
}

message UpdateMetricResponse {
    Metric metric = 1;
    int32 status = 2;
}

message UpdateMetricsRequest {
    repeated Metric metrics = 1;
}

message UpdateMetricsResponse {
    repeated Metric metrics = 1;
    int32 status = 2;
}

message ListAllMetricsRequest {
}

message ListAllMetricsResponse {
  repeated Metric metrics = 1;
}

message GetMetricRequest {
  Metric metric = 1;
}

message GetMetricResponse {
  Metric metric = 1;
  int32 status = 2;
}

service MetricsService {
  rpc CheckConnection(CheckConnectionRequest) returns (CheckConnectionResponse);
  rpc UpdateMetric(UpdateMetricRequest) returns (UpdateMetricResponse);
  rpc UpdateMetrics(UpdateMetricsRequest) returns (UpdateMetricsResponse);
  rpc ListAllMetrics(ListAllMetricsRequest) returns (ListAllMetricsResponse);
  rpc GetMetric(GetMetricRequest) returns (GetMetricResponse);
}
